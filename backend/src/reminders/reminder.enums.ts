export enum ReminderRecurrence {
  NONE = 'none',
  DAILY = 'daily',
  WEEKLY = 'weekly',
  MONTHLY = 'monthly',
}

export enum ReminderStatus {
  PENDING = 'pending',
  SENT = 'sent',
  CANCELLED = 'cancelled',
}

export enum ReminderNotificationType {
  WHATSAPP = 'whatsapp',
  SMS = 'sms',
  BOTH = 'both',
}

